```
├── default.conf        // "web" container nginx configuration file
├── docker-compose.yml
└── web
    ├── admin
    │   └── index.html // "admin.web.local" document root
    └── index.html      // "web.local" document root
```
